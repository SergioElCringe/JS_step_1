function plus() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a + b).toFixed(6);
}
function minus() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a - b).toFixed(6);
}
function division() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a / b).toFixed(6);
}
function multiply() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a * b).toFixed(6);
}