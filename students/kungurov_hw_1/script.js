function plus() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a + b)
}
function minus() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a - b)
}
function division() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a / b)
}
function multiply() {
    var a = Number(prompt());
    var b = Number(prompt());
    console.log(a * b)
}